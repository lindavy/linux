# HW 1: Discovering VMX Features of Intel Processor

## Run 1st Module
Try running "Hello World" module. It is in the 'linux/kernel/hello' directory. 
```
make
```

Check the output at the end of '/var/log/kern.log'
```
tail /var/log/kern.log
```

